apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "adp.fullname" . }}
  labels:
    {{- include "adp.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "adp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "adp.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.configmap.enabled }}
      volumes:
        - name: config-volume
          configMap:
            name: {{ if .Values.configmap.name }}{{ .Values.configmap.name }}{{ else }}{{ include "adp.fullname" . }}-demo-b-config{{ end }}
      {{- end }}
      initContainers:
        - name: wait-for-demo-a
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for demo-a service to be available..."
              until nc -z test-demo-a 8080; do
                echo "demo-a service is not ready yet, waiting..."
                sleep 5
              done
              echo "demo-a service is ready! Starting demo-b..."
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: LOCAL_CONFIG_PATH
              value: {{ .Values.configmap.mountPath }}/{{ .Values.configmap.subPath }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /getconfig
              port: {{ .Values.service.port }}
          readinessProbe:
            httpGet:
              path: /getconfig
              port: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.configmap.enabled }}
          volumeMounts:
            - name: config-volume
              mountPath: {{ .Values.configmap.mountPath }}
          {{- end }}