app_name: golang-demo-test
version: "1.0.0"

database:
  host: localhost
  port: 5432
  username: admin
  password: secret

server:
  port: 8080
  timeout: 30

features:
  - auth
  - logging
  - caching